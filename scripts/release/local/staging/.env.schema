# Local staging provisioning only; values resolve through the 1Password desktop app.
# @defaultSensitive=true
# @plugin(@varlock/1password-plugin)
# @initOp(allowAppAuth=true, account=my.1password.com, cacheTtl=false)
# ----------

# @sensitive @required
STAGE_COLLABORATION_CAPABILITY_CURRENT_SECRET=op(op://dev/ephemeral-pages/STAGE_COLLABORATION_CAPABILITY_CURRENT_SECRET)

# @sensitive @required
STAGE_COLLABORATION_TICKET_SECRET=op(op://dev/ephemeral-pages/STAGE_COLLABORATION_TICKET_SECRET)

# @sensitive @required
STAGE_COLLABORATION_SERVICE_TOKEN=op(op://dev/ephemeral-pages/STAGE_COLLABORATION_SERVICE_TOKEN)
